# Copy to group_vars/all.yml, fill in, then (optionally): ansible-vault encrypt group_vars/all.yml
# The glockpeek binary is built locally from your working tree and shipped over,
# so there's nothing here about repos or Go versions.

# Public dashboard domain (must resolve to the droplet).
domain: "peek.example.com"

# Postgres.
pg_db: "glockpeek"
pg_user: "glockpeek"
pg_password: "CHANGE-ME"        # secret — vault this

# First dashboard account (created by the playbook).
admin_user: "noufal"
admin_password: "CHANGE-ME"     # secret — vault this

# Outbound email for account verification (Mailgun). SERVER-SIDE ONLY — this key
# never leaves the glockpeek host. region is "us" or "eu" (match your Mailgun domain).
mail_enabled: true
mail_domain: "mg.glockerapp.com"
mail_api_key: "CHANGE-ME"       # secret — vault this
mail_from: "noreply@mg.glockerapp.com"
mail_region: "us"
